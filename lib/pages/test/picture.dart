import 'package:flutter/material.dart';

class PictureTest extends StatelessWidget {
  const PictureTest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}